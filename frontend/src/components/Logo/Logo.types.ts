export interface LogoProps {
  /**
   * Size variant of the logo
   * @default "full"
   */
  size?: 'full' | 'compact';

  /**
   * Text color variant of the logo
   * @default "default"
   */
  textColor?: 'default' | 'white';
}
