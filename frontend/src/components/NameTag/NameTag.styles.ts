import { tv } from 'tailwind-variants';

export const NameTagStyles = tv({
  base: 'flex items-center justify-center w-[145px] h-[36px] bg-[#DADADA]  rounded-[4px] px-[15px] py-[10px] text-[#757575] text-[14px] leading-none font-normal truncate',
});
